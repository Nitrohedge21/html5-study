	Notes I'm taking for the HTML5 study.
    
    <!--This is an HTML comment-->


// "title" is used to display a text upon hovering on the chosen text/button
<h1 title="Balls"> Placeholder Text	</h1> 

// "style" can be used for many things(?) like opacity for example
<h1 style="opacity: 0.5;"> Placeholder Text	</h1> 

// This creates a button that is linked to google.com
<body>
		<a href="https://google.com">
			<button"> Testing Button </button> 
		</a>
</body>

// This can be used to give spacing on the chosen direction
<a> style="margin-bottom: 10px;"

// <div> can be used for just one line/phrase/sentence with custom attributes

// This is used to make the website scale based on your device
<meta name="viewport" content="width=device-width, initial-scale=1.0">

// <strong> or <b> can be used to make a text bold

// <i> or <em> can be used to tilt the text

// <u> adds a line under the text

// <mark> fosforlu kalem

// <sub> or <sup> Moves the text up/down on the same line

// <code> is used to make text appear inside a code block

// <span> can be used to colour a specific part of a sentence
<p>My mother has <span style="color:blue">blue</span> eyes.</p>

// The "method" can either be GET or POST. Post keeps information hidden
<form name="sign-up-form" action="signup.php" method="post">

// autoplay attribute doesn't work if the video isn't muted.
<video src="videos/demo.mp4" controls autoplay muted height="500">

// No clue if " type="video/mp4" " is necessary to define as the file is already doing that
